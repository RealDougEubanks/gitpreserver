<?xml version="1.0"?>
<Container version="2">
  <Name>GitPreserver</Name>
  <Repository>dougeubanks/gitpreserver</Repository>
  <Registry>https://hub.docker.com/r/dougeubanks/gitpreserver</Registry>
  <Network>bridge</Network>
  <Privileged>false</Privileged>
  <Support>https://forums.unraid.net/topic/gitpreserver</Support>
  <Project>https://github.com/RealDougEubanks/gitpreserver</Project>
  <Overview>
    A life preserver for your git repositories. Mirror-clones all repos with full branch and tag
    history, exports issues/PRs/releases as JSON, and (optionally) syncs encrypted backups to
    Backblaze B2, AWS S3, Google Drive, OneDrive, MEGA, SMB, or any rclone-supported destination.

    Two run modes:
    - oneshot (default): run a backup and exit. Trigger via User Scripts or docker start.
    - daemon: stay running, run backups on an internal cron schedule, and serve a web status UI
      on port 6033. Visit http://&lt;unraid-ip&gt;:6033/ to see last backup status and trigger a manual run.

    See the project README and docs/daemon-mode.md for full setup.
  </Overview>
  <Category>Backup: Tools:</Category>
  <Icon>https://raw.githubusercontent.com/RealDougEubanks/gitpreserver/main/assets/icon.png</Icon>
  <ExtraParams></ExtraParams>
  <PostArgs></PostArgs>
  <DateInstalled></DateInstalled>
  <Beta>False</Beta>

  <!--
    Git host selection. GitHub is the default.
    For self-hosted GitLab or Gitea, also set the Host URL below.
  -->
  <Config Name="Git Host" Target="GITPRESERVER_HOST_TYPE" Default="github"
    Mode="" Description="Which git hosting service to back up: github | bitbucket | gitlab | gitea"
    Type="Variable" Display="always" Required="false" Mask="false"/>

  <Config Name="Host URL (self-hosted only)" Target="GITPRESERVER_HOST_URL" Default=""
    Mode="" Description="Base URL for self-hosted GitLab or Gitea (e.g. https://gitlab.mycompany.com). Leave blank for github.com, bitbucket.org, or gitlab.com."
    Type="Variable" Display="advanced" Required="false" Mask="false"/>

  <!--
    Personal Access Token.
    GitHub: fine-grained PAT (github_pat_...) with Contents, Metadata, Issues, Pull requests (Read).
    Bitbucket: app password with Repositories (Read) and Issues (Read) scopes.
    GitLab: personal access token with read_api scope.
    Gitea: personal access token.
  -->
  <Config Name="Personal Access Token" Target="GITPRESERVER_TOKEN" Default=""
    Mode="" Description="Personal access token for your git host. GitHub: fine-grained PAT recommended. Bitbucket: app password. GitLab: personal access token with read_api scope."
    Type="Variable" Display="always" Required="true" Mask="true"/>

  <Config Name="Username or Org" Target="GITPRESERVER_USERNAME" Default=""
    Mode="" Description="Username or organization name to back up on the selected git host."
    Type="Variable" Display="always" Required="true" Mask="false"/>

  <Config Name="Backup Path" Target="/backups" Default="/mnt/user/appdata/gitpreserver/backups"
    Mode="rw" Description="Host path where dated backup snapshots are written. Each run creates a YYYY-MM-DD subdirectory containing repos/ and metadata/."
    Type="Path" Display="always" Required="true" Mask="false"/>

  <!-- Run mode -->
  <Config Name="Mode" Target="GITPRESERVER_MODE" Default="oneshot"
    Mode="" Description="oneshot: run a backup and exit. daemon: stay running, back up on a schedule, and serve the web UI on port 6033."
    Type="Variable" Display="always" Required="false" Mask="false"/>

  <!--
    rclone remote sync. Leave 'rclone Remote' blank to keep backups local only,
    in which case the rclone.conf path below is ignored.
  -->
  <Config Name="rclone Remote" Target="GITPRESERVER_RCLONE_REMOTE" Default=""
    Mode="" Description="rclone remote name (e.g. b2-remote). Must exist in rclone.conf below. Leave blank for local-only backup."
    Type="Variable" Display="always" Required="false" Mask="false"/>

  <Config Name="rclone Bucket / Path" Target="GITPRESERVER_RCLONE_PATH" Default="gitpreserver-backups"
    Mode="" Description="Path or bucket on the rclone remote to sync into."
    Type="Variable" Display="always" Required="false" Mask="false"/>

  <Config Name="rclone Config File" Target="/home/gitpreserver/.config/rclone/rclone.conf" Default="/mnt/user/appdata/gitpreserver/rclone.conf"
    Mode="ro" Description="Host path to your rclone.conf. Mounted read-only. Only required when an rclone remote is configured above."
    Type="Path" Display="always" Required="false" Mask="false"/>

  <!-- Encryption (rclone crypt) -->
  <Config Name="Enable Encryption" Target="GITPRESERVER_ENCRYPT" Default="false"
    Mode="" Description="Set to 'true' to encrypt backups via rclone crypt. Requires the Crypt Remote below to be configured in rclone.conf."
    Type="Variable" Display="advanced" Required="false" Mask="false"/>

  <Config Name="Crypt Remote" Target="GITPRESERVER_CRYPT_REMOTE" Default=""
    Mode="" Description="Name of an rclone crypt remote that wraps your storage remote. Required when Enable Encryption is true."
    Type="Variable" Display="advanced" Required="false" Mask="false"/>

  <!-- Retention -->
  <Config Name="Retention Days" Target="GITPRESERVER_RETENTION_DAYS" Default="30"
    Mode="" Description="Delete local dated snapshot directories older than this many days. Set to 0 to keep forever."
    Type="Variable" Display="advanced" Required="false" Mask="false"/>

  <!-- Host UID / GID — required so the container can write to /mnt/user paths -->
  <Config Name="PUID" Target="PUID" Default="99"
    Mode="" Description="Host user ID for file ownership. unRAID's default 'nobody' user is UID 99."
    Type="Variable" Display="advanced" Required="false" Mask="false"/>

  <Config Name="PGID" Target="PGID" Default="100"
    Mode="" Description="Host group ID for file ownership. unRAID's default 'users' group is GID 100."
    Type="Variable" Display="advanced" Required="false" Mask="false"/>

  <Config Name="UMASK" Target="UMASK" Default="022"
    Mode="" Description="File creation mask for backup files."
    Type="Variable" Display="advanced" Required="false" Mask="false"/>

  <Config Name="Schedule (daemon mode)" Target="GITPRESERVER_SCHEDULE" Default="0 2 * * 0"
    Mode="" Description="Cron expression for automatic backups in daemon mode. Default: Sundays at 02:00. Ignored in oneshot mode."
    Type="Variable" Display="advanced" Required="false" Mask="false"/>

  <Config Name="Web UI Port" Target="GITPRESERVER_WEB_PORT" Default="6033"
    Mode="" Description="Port for the daemon-mode web status UI. Only active when Mode is 'daemon'."
    Type="Variable" Display="advanced" Required="false" Mask="false"/>

  <Config Name="Web UI" Target="6033" Default="6033"
    Mode="tcp" Description="Status dashboard and manual backup trigger. Only active in daemon mode."
    Type="Port" Display="advanced" Required="false" Mask="false"/>

  <!-- Operational toggles -->
  <Config Name="Log Level" Target="GITPRESERVER_LOG_LEVEL" Default="info"
    Mode="" Description="Log verbosity passed to rclone: debug | info | warn | error"
    Type="Variable" Display="advanced" Required="false" Mask="false"/>

  <Config Name="Dry Run" Target="GITPRESERVER_DRY_RUN" Default="false"
    Mode="" Description="Set to 'true' to test without writing repos or syncing. Useful for verifying credentials and paths the first time."
    Type="Variable" Display="advanced" Required="false" Mask="false"/>

</Container>
